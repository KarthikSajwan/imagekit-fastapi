ImageKit + FastAPI Media Upload App

A full-stack media upload application built with FastAPI, JWT authentication, ImageKit, and a Streamlit frontend.

Users can:

Register & log in securely
Upload images or videos
View only their own uploads
Delete their own media
Access everything via JWT-protected APIs
